# ══════════════════════════════════════════════
# JARVIS v4.0 Requirements
# Install: pip install -r requirements.txt
# ══════════════════════════════════════════════

# ── Claude API (Core) ──────────────────────────
anthropic>=0.28.0

# ── Voice I/O ─────────────────────────────────
edge-tts>=6.1.12
pygame>=2.6.1
SpeechRecognition>=3.10.0
sounddevice>=0.4.6
webrtcvad>=2.0.10          # VAD (silence detection)
pyaudio>=0.2.14            # optional fallback for SpeechRecognition

# ── Audio processing ──────────────────────────
numpy>=1.24.0
scipy>=1.11.0

# ── Memory & Encryption ───────────────────────
cryptography>=42.0.0       # Fernet field encryption for memory.db

# ── Smart Home ────────────────────────────────
tinytuya>=1.12.8           # Tuya smart plugs/lights

# ── Async HTTP ────────────────────────────────
aiohttp>=3.9.0

# ── Web server (Alexa bridge, optional) ───────
flask>=3.0.0

# ── PC Automation ─────────────────────────────
psutil>=5.9.0
pyautogui>=0.9.54

# ── Dashboard ─────────────────────────────────
streamlit>=1.30.0

# ── RAG / Embeddings ─────────────────────────
chromadb>=0.4.22

# ── Utils ─────────────────────────────────────
python-dotenv>=1.0.1

# ── Wake word (optional) ──────────────────────
openwakeword>=0.6.0

# ── v3 compatibility ─────────────────────────
faster-whisper>=1.0.0
thefuzz>=0.20.0
python-Levenshtein>=0.21.0
requests>=2.31.0
feedparser>=6.0.11           # RSS parsing for news scraper

# ── Google Calendar (optional) ────────────────────────────────────
google-api-python-client>=2.100.0   # Google Calendar API
google-auth-oauthlib>=1.1.0         # OAuth2 flow
google-auth-httplib2>=0.1.1         # HTTP transport
